import { Avatar, Dropdown } from "@sneyed/zenith";
import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";

<Meta title="Components/Dropdown" component={Dropdown} />

# Dropdown

Description

<Canvas>
  <Story name="Default">
    <div style={{ paddingBottom: 84 }}>
      <Dropdown
        items={[
          { label: "Profile", url: "#profile" },
          { label: "Settings", url: "#settings" },
          { label: "Sign out", url: "#signout" },
        ]}
      />
    </div>
  </Story>
</Canvas>

| Property | Type             | Required | Description                                                           | Default                    |
| -------- | ---------------- | -------- | --------------------------------------------------------------------- | -------------------------- |
| label    | string           | no       | Text to display on button face                                        | "Dropdown"                 |
| button   | jsx              | no       | Component(s) to display instead of button. Overrides `label` property | `<Button label={label} />` |
| items    | `[DropdownItem]` | no       | Array of `DropdownItem` properties                                    | `undefined`                |
| children | jsx              | no       | Component(s) to display instead of `items`                            | `undefined`                |

# Dropdown Item

Description

| Property | Type   | Required | Description                                | Default     |
| -------- | ------ | -------- | ------------------------------------------ | ----------- |
| label    | string | no       | Text to display for item                   | "Item"      |
| url      | string | no       | Url to navigate to when item is clicked    | `undefined` |
| children | jsx    | no       | Component(s) to display instead of `label` | `undefined` |

## Dropdown Examples

### Align

<Canvas>
  <Story name="Align">
    <div
      style={{
        display: "flex",
        justifyContent: "space-around",
        width: "100%",
        paddingBottom: 84,
      }}
    >
      <Dropdown
        align="left"
        items={[
          { label: "Profile", url: "#profile" },
          { label: "Settings", url: "#settings" },
          { label: "Sign out", url: "#signout" },
        ]}
      />
      <Dropdown
        align="right"
        items={[
          { label: "Profile", url: "#profile" },
          { label: "Settings", url: "#settings" },
          { label: "Sign out", url: "#signout" },
        ]}
      />
    </div>
  </Story>
</Canvas>

### Avatar as button

<Canvas>
  <Story name="Avatar Button">
    <div style={{ paddingBottom: 84 }}>
      <Dropdown
        button={
          <Avatar name="Loki McKay" src="https://i.imgur.com/niM85vH.png" />
        }
        items={[
          { label: "Profile", url: "#profile" },
          { label: "Settings", url: "#settings" },
          { label: "Sign out", url: "#signout" },
        ]}
      />
    </div>
  </Story>
</Canvas>

### With custom children

<Canvas>
  <Story name="Custom Children">
    <div style={{ paddingBottom: 96 }}>
      <Dropdown
        items={[
          { children: <div>Profile</div> },
          { children: <span>Settings</span> },
          { children: <p>Sign out</p> },
        ]}
      />
    </div>
  </Story>
</Canvas>
