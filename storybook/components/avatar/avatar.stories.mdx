import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs/blocks";
import { Avatar } from "@sneyed/zenith";
import { getArgTypes } from "../../lib/util";

export const argTypes = [
  {
    name: "size",
    description: "CSS width and height attribute",
  },
  {
    name: "src",
    description: "Image's url",
  },
  {
    name: "shape",
    description: "Circular or square appearance",
    control: {
      type: "select",
      options: ["circle", "square"],
    },
  },
];

<Meta
  title="Components/Avatar"
  component={Avatar}
  argTypes={getArgTypes(argTypes)}
/>

# Avatar

Image element that typically represents a user

<Story name="Default">{args => <Avatar {...args} />}</Story>

<ArgsTable story="Default" />
