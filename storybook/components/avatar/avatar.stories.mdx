import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs/blocks";
import Avatar from "@sneyed/zenith/src/components/avatar";
import { getArgTypes } from "../../lib/util";

export const properties = [
  {
    name: "name",
    description: "Accessibility identifier",
  },
  {
    name: "url",
    description: "Url to navigate to on click",
  },
  {
    name: "src",
    description: "`<img>` element's `src` attribute",
  },
  {
    name: "size",
    description: "`width` and `height` CSS attributes",
  },
  {
    name: "borderRadius",
    description: "`border-radius` CSS attribute",
  },
  {
    name: "className",
    description: "Provide custom css classes to the `<img>` element",
  },
];

<Meta
  title="Components/Avatar"
  component={Avatar}
  argTypes={getArgTypes(properties)}
/>

# Avatar

Image element that typically represents a user

<Story name="Default">{args => <Avatar {...args} />}</Story>

```jsx
import { Avatar } from "@sneyed/zenith";

export default function MyComponent() {
  return (
    <Avatar
      name="profile"
      url="https://github.com/sneyed"
      src="https://i.imgur.com/niM85vH.png"
      size="64px"
      borderRadius="50%"
    />
  );
}
```

<ArgsTable story="Default" />
